create_dataset:
  # Input filepaths
  transcripts_parquet: '/data1/peerd/moormana/GitHub/EliHei2/segger_dev/.dev/data/cosmx_human_pancreas_healthy/ist_dataset/transcripts_geo.parquet'
  boundaries_parquet: '/data1/peerd/moormana/GitHub/EliHei2/segger_dev/.dev/data/cosmx_human_pancreas_healthy/ist_dataset/nucleus_boundaries_geo.parquet'
  # Input fields
  transcripts_feature_name: 'target'
  transcripts_cell_id: 'nucleus_boundaries_id'
  transcripts_x: 'x_global_px'
  transcripts_y: 'y_global_px'
  # Graph params
  transcripts_k: 20
  transcripts_dist: 5
  boundaries_k: 3
  boundaries_dist: 20
  # Tiling params
  max_cells_per_tile: 50
  tile_margin: 75
  negative_sampling_ratio: 5
  fraction_train: 0.7
  fraction_test: 0.2
  fraction_val: 0.1
  # Ouput options
  save_dir: '/data1/peerd/moormana/GitHub/EliHei2/segger_dev/.dev/data/cosmx_human_pancreas_healthy/segger_dataset'
  n_workers: 8